# A：シグナル条件の改訂（勝率改善ルールの実装）

分析（`勝率改善分析レポート.md`）の結論を、実際のシグナル選定に反映します。
**変えるのはたった2セル**（全体売銘柄・全体買銘柄の `B2`）。土台となる
**市場/貸借マスターは `打ち出のこづち_V0728_改修版.xlsm` の「銘柄リスト」に作成済み**です。

---

## 何が変わるか（現行 → 改訂）

| 観点 | 現行 | 改訂後 |
|---|---|---|
| エントリー閾値 | なし（少しでも動けば候補） | **\|15:00→15:20\| ≥ 0.8%** |
| 相場環境 | 無視 | **TOPIX前日比率 > 0（上げ相場の日）だけ**。下げ日は候補ゼロ＝ノートレード |
| 銘柄の質 | 全銘柄 | **東証プライムのみ**（売りは加えて**貸借銘柄のみ**＝空売り執行可能） |

> 前提：「銘柄リスト」に B列=市場、C列=貸借区分 が入っていること（改修版に作成済み）。

---

## 手順（コピペ2か所）

### ① 「全体売銘柄」シートの B2 に貼り付け

```
=LET(mv,$S$2:$S$94,
 mk,XLOOKUP($P$2:$P$94,銘柄リスト!$A$2:$A$94,銘柄リスト!$B$2:$B$94,""),
 tk,XLOOKUP($P$2:$P$94,銘柄リスト!$A$2:$A$94,銘柄リスト!$C$2:$C$94,""),
 cond,(mv>=0.008)*(mk="東Ｐ")*(tk="貸借"),
 IF(関数!$C$1<=0,"",IFERROR(SORTBY(FILTER($P$2:$P$94,cond),FILTER(mv,cond),-1),"")))
```

### ② 「全体買銘柄」シートの B2 に貼り付け

```
=LET(mv,$S$2:$S$94,
 mk,XLOOKUP($P$2:$P$94,銘柄リスト!$A$2:$A$94,銘柄リスト!$B$2:$B$94,""),
 cond,(mv<=-0.008)*(mk="東Ｐ"),
 IF(関数!$C$1<=0,"",IFERROR(SORTBY(FILTER($P$2:$P$94,cond),FILTER(mv,cond),1),"")))
```

貼り付け後 **Enter** → 下方向へ自動でスピル（C〜I列は既存数式が追従）。
サマリーの売り/買いシグナルも自動更新されます。

---

## 動作の読み方

- **TOPIXがマイナスの日**：売り・買いとも候補が**空**になります＝「今日は張らない」の合図
  （分析上、下げ日の逆張り、特に押し目買いは勝率が大きく落ちるため）。
- **TOPIXがプラスの日**：0.8%以上動いたプライム銘柄だけが、動いた順に並びます。
  売りは貸借銘柄に限定（空売り可能）。

---

## 微調整（お好みで）

- もっと厳選したい：`0.008` を **`0.01`（1.0%）** に。件数は減るが平均的に堅くなる。
- 上げ相場をより明確に:`関数!$C$1<=0` を **`関数!$C$1<=0.003`（+0.3%以下は見送り）** に。
- ★数値の意味：`mv` は比率なので **0.008＝0.8%**、`関数!$C$1` は%数値なので **>0 が上げ日**。

---

## 補足（重要）

- サマリーの「勝率予想／平均利幅」列（H列）は、旧バックテスト表（関数!H:M）を参照しています。
  §8 の通りこの旧統計は信頼性が低いので**あくまで目安**。選定ロジック自体は上の B2 が担うので、
  勝率改善には直接効きます。旧統計表の作り替えが必要なら別途対応します。
- これは過去データに基づく設計で、**将来の勝率を保証しません**。まず少額/ペーパーで検証を。
