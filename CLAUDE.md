# このリポジトリでの作業ルール

## 1. コードは必ず「完全版ファイル」で渡す ★最優先

**部分修正の貼り付け指示は禁止。**

- 「この行を◯◯に変えてください」「ここに以下を追記してください」という
  差分形式の指示は**出さない**
- 対象モジュールの**先頭から末尾までの完全なコード**をファイルとして用意し、
  リポジトリに置いたうえでユーザーに渡す
- ユーザー側の操作は「全選択して差し替える」または「インポートする」だけで
  完結すること。どこに何を入れるかを判断させない

理由: 部分修正は貼り付け位置を誤りやすく、重複定義や欠落を生む。
実際に `Workbook_BeforeClose` の二重定義でブック全体のマクロが停止した。

### 例外なし

「1行だけ」でも完全版ファイルを出す。

## 2. VBA ファイルの文字コードは Shift-JIS (CP932) + CRLF

VBE の「ファイル → ファイルのインポート」は `.bas` / `.cls` を
**CP932 として読む**。UTF-8 で渡すと日本語コメント・文字列が文字化けし、
壊れたバイト列が文字列リテラルを破壊して
**「コンパイル エラー: 構文エラー」**になる。

- ユーザーに渡すファイルは **CP932 + CRLF** で生成する
- GitHub 上で読ませたい UTF-8 版を別に置く場合は、
  ファイル名かディレクトリで明確に区別する（`vba/` = UTF-8、`vba/sjis/` = CP932）

変換コマンド:

```bash
iconv -f UTF-8 -t CP932 input.bas | sed 's/$/\r/' > output.bas
```

## 3. ドキュメントモジュールは import できない

`ThisWorkbook` や `Sheet1` などのドキュメントモジュールは、
`.cls` をインポートしても**新しいクラスモジュールが増えるだけ**で
差し替えにならない。

- これらは `.txt` として完全版を渡し、
  「コードウィンドウで `Ctrl+A` → 貼り付け」で差し替えてもらう
- 渡す前に、そのモジュールに**他のプロシージャが無いか必ず確認する**
  （`Ctrl+A` は全消しになるため）

標準モジュール（`Module1` 等）は `.bas` でインポート可。

## 4. 解析対象を取り違えない

リポジトリにあるファイルと、ユーザーが困っているファイルが
**同じとは限らない**。

- 対象ファイルがリポジトリに無い場合は、**代用して断定しない**
- 「リポジトリに無いので、実ファイルかエクスポートしたコードが必要」と
  先に伝える

実際に `打ち出のこづち_0615MOD整理 改善 .xlsm` を
`0945_baibai.xlsm` の代用として解析し、結論を誤った。

## 5. コードを受け取る方法

ユーザーからコードをもらうときは、VBE の
**右クリック → ファイルのエクスポート** で `.bas` / `.cls` を書き出して
アップロードしてもらう。チャットへの貼り付けは省略や化けが起きやすい。

---

# ファイル構成

| パス | 内容 |
|---|---|
| `docs/` | 調査レポート・手順書 |
| `vba/` | VBAモジュール（UTF-8・GitHub閲覧用） |
| `vba/sjis/` | 同内容の CP932 + CRLF 版（VBEインポート用） |
| `vba/0945_baibai/` | `0945_baibai.xlsm` 専用の完全版モジュール |
| `tools/` | 診断用スクリプト |

# 対象ブック

| ブック | 状況 |
|---|---|
| `打ち出のこづち_0615MOD整理 改善 .xlsm` | 解析済み。`Application.OnTime` 8箇所、`Workbook_BeforeClose` 無し。`vba/Fix_OnTime_Cleanup.bas` が対策 |
| `0945_baibai.xlsm` | **リポジトリに未登録**（コードのみ受領）。`Module1` は空、`Module2`（出来高モニター v7.2）に `Application.OnTime` が4箇所。`VolTick` が `IsRunning` 判定より前に無条件で再予約し、`Workbook_BeforeClose` が予約を解除しないため自己再オープンしていた。**原因特定済み**。`vba/0945_baibai/MOD2_出来高モニター_v7.3.txt` が対策 |
