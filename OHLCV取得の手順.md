# OHLCV 250日分の取得手順

> ## 先にお読みください（2つの不具合を修正しました）
>
> **① 文字化け** — `.bas` を UTF-8 で作っていたのが原因でした。
> VBAエディタは日本語Windowsで **Shift-JIS(CP932)** として読むため、
> 日本語コメントが全部化けていました。**Shift-JISで保存し直しました。**
>
> **② コンパイルエラー（修正候補: 変数）** — 変数名 `cDate` が
> VBA組み込み関数 **`CDate`** と衝突していました。VBAは大文字小文字を
> 区別しないので同じ名前とみなされます。
> 同様に `mid`（組み込み `Mid`）、`name`（組み込み `Name`）も衝突していたため、
> **3つとも改名しました**（`colDate` / `midIdx` / `shName`）。
>
> **モジュールが2つある場合**（`modFetch250` と `modFetch2501`）は、
> 両方とも右クリック →「解放」→「いいえ」で消してから、
> 新しい `.bas` を **1回だけ** インポートしてください。
>
> インポート後は **デバッグ → VBAProject のコンパイル** で
> エラーが出ないことを確認してください。

`tools/modFetch250.bas` を使います。**取得から書き込みまで全自動**です。

---

## 1. 取り込む

### 準備

1. マーケットスピードを起動してログインしておく
2. V811.xlsm を開く
3. `Alt + F11` でVBAエディタを開く
4. 左のツリーで **`VBAProject (V811.xlsm)` をクリックして選択**
5. 既に `modFetch250` や `modFetch2501` があれば、**すべて右クリック →「解放」→「いいえ」**で消す
6. メニュー **ファイル → ファイルのインポート**（`Ctrl+M`）で `modFetch250.bas` を選ぶ（**1回だけ**）
7. **デバッグ → VBAProject のコンパイル** でエラーが出ないことを確認
8. `Alt + F11` でExcelに戻り `Ctrl + S` で保存 → `Alt + F8` でマクロ一覧

### 実行の順番（★ここが大事）

**V805から引き継いだ日付軸は 74日分（2026/04/14〜08/06）しかありません。**
`OHLCV_1銘柄取得` は「いまシートにある日付軸」をそのまま使うので、
**4/14より前の日付は、取得できていても書き込まれません**。

250日にするには、この順番で実行してください。

| 順 | マクロ名 | 内容 |
|---|---|---|
| **1** | **`OHLCV_日付軸を作り直す`** | 軸を250日に張り替える（**既存データは全部消えます**） |
| **2** | **`OHLCV_全銘柄取得`** | 全銘柄を新しい軸で取り直す（10〜30分） |
| 随時 | `OHLCV_取得状況を確認` | 何銘柄そろっているか表示 |
| 随時 | `OHLCV_1銘柄取得` | 1銘柄だけ入れ直す（軸は既存のまま使う） |

> **なぜ軸を張り替えると既存データを消すのか**
>
> 古いデータは「古い軸の並び」で書かれています。軸だけ差し替えると
> **同じ列が違う日付を指す**ことになり、銘柄間の比較が静かに壊れます。
> 消してしまえば、取得できなかった銘柄は空欄として残るので気づけます。

`OHLCV_全銘柄取得` を実行すると、確認ダイアログのあと自動で回ります。

---

## 2. 何をしているか

データ取込シートに元からある仕組みをそのまま回しています。

```
A3 = RssChartPast(B2:J2, B1, "D", $M$1, $K$1)
       B1 = 銘柄コード     K1 = 本数(250)     M1 = 開始日(今日)
```

1. `K1=250` / `M1=今日` をセット
2. **日付軸を作る** … TOPX の日足から250営業日分の日付を取り、5シートの3行目に書く
3. 銘柄管理 B6:B305 を上から順に
   - B1 に銘柄コードを入れる → 再計算 → RSSの応答を待つ
   - **日付を突き合わせて** 始値/高値/安値/終値/出来高 の5シートに書く

### 日付で突き合わせて書く理由

V805 の取込は「E列＝最新、F列＝1日前…」と**順番に詰めていく**方式でした。

この方式だと、売買停止などで1日抜けた銘柄だけ列が1つズレます。すると
**銘柄どうしで同じ列が違う日付を指す**ことになります。V811の指標は
すべて銘柄間の比較（順位）で計算するので、ズレると結果が壊れます。

V811では3行目の日付軸に対して日付を突き合わせ、**同じ日付の列に書きます**。
軸に無い日付は書きません（欠測として空欄のまま）。

```
日付軸    8/6  8/5  8/4  8/3  7/31  7/30 …
銘柄A     100  101  102  103   104   105    ← 全部そろっている
銘柄B     200  201       203   204   205    ← 8/4が売買停止 → その列だけ空欄
                  ↑ ここがズレない
```

### RSSの応答待ち

RSSは非同期なので、値が届くまで待つ必要があります。

- **銘柄名の列が目的の銘柄になっている**
- **かつ行数が3回続けて同じ**

この両方がそろって初めて「受信完了」とみなします。
行数だけで判断すると、連続する2銘柄がたまたま同じ日数のとき
**前の銘柄の古いデータをそのまま読んでしまう**ためです。

25秒待っても応答がなければ、その銘柄は失敗として次に進みます。

---

## 3. 設定（モジュール冒頭で変更できます）

```vba
Private Const HIST_MAX     As Long = 250     ' 取得する営業日数
Private Const WAIT_LIMIT   As Double = 25    ' RSS応答の待ち時間の上限（秒）
Private Const STABLE_POLLS As Long = 3       ' この回数だけ内容が変わらなければ受信完了
Private Const POLL_WAIT    As Double = 0.2   ' 1回の待ち（秒）
Private Const SKIP_IF_HAVE As Long = 0       ' 既にこの日数以上ある銘柄を飛ばす（0=飛ばさない）
```

- **250日が取れない場合** → `HIST_MAX` を 150 などに下げてください。楽天から取れる範囲で構いません
- **失敗が多い場合** → `WAIT_LIMIT` を 40 くらいに増やしてください
- **2回目以降で失敗分だけ埋めたい場合** → `SKIP_IF_HAVE = 200` にすると、
  200日以上そろっている銘柄を飛ばすので速く終わります

---

## 4. 取り込んだあと

1. `OHLCV_取得状況を確認` で件数をチェック

```
登録銘柄:        300
  100日以上:     296
  100日未満:       4
  データなし:       0
日付軸(3行目):   250 営業日分
最少の銘柄:      118 日分
```

2. `F9` で再計算 → `V811指標` シートに指標とスコアが出ます
3. `厳選TOP2` シートに買い候補が 0〜3件 出ます

**指標の計算には25日以上、検証には100日以上**あれば動きます。
150日でも、いまの74日から比べれば検証力は倍以上になります。

---

## 5. 中断・エラーについて

途中で止まっても、`Application.Calculation` は必ず元に戻します。

V805 の抽出マクロ4本にはこの後始末が無く、途中でエラーが起きると
計算が手動モードのまま残り、**現在値が更新されず損切判定が完全に沈黙する**
状態になっていました。このモジュールでは ESC 中断・エラー・正常終了の
いずれの場合も、計算モード・画面更新・ステータスバーを復元します。

---

## 6. 検証済みの項目

このモジュールについて、静的に確認できる範囲は検査済みです。

```
Sub/Function      11/11, 9/9        対応OK
Do/Loop           4/4               対応OK
For/Next          18/18             対応OK
If/End If         27/27             対応OK
With / Select     2/2, 2/2          対応OK
GoTo とラベル      未定義への分岐なし / 未使用ラベルなし
Option Explicit   あり
内部呼び出し       未定義の手続きなし（20手続き）
日付軸の二分探索   250日で全件が正しい位置を返す。軸に無い日付は0を返す
                  1日欠測の銘柄でも他銘柄と列がズレないことを確認
```

ただし **実機のExcel/RSSでの動作確認はできていません**（この環境に
Excelもマーケットスピードもないため）。初回は1銘柄（`OHLCV_1銘柄取得`）で
試してから全銘柄を回してください。
