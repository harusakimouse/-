# 入れ方

## 全体の流れ

```
1. V812.xlsm を開く          ← 数式はもう250日対応済み
2. VBA 5本を入れ替える        ← 取込を250日にする
3. データ取込シートの K1 / M1 を設定
4. 銘柄を取り込む
```

---

## 1. V812.xlsm を開く

ダウンロードした `V812.xlsm` を、いつも使っているフォルダに置いて開きます。

- **「編集を有効にする」** → クリック
- **「コンテンツの有効化」**（マクロの警告） → クリック

> V812 は V811 から作っています。V811 に自分でインポートした
> `modFetch250` は入っていません。必要なら別途インポートしてください。

**開いた直後に、いったん `Ctrl + S` で保存してください。** ここで壊れずに
保存できれば、数式の修正は問題なく入っています。

---

## 2. VBA を入れ替える

`Alt + F11` でVBAエディタを開きます。

### 2-1. 古いモジュールを消す（★先に消す）

左のツリーの **標準モジュール** の中から、次の4つを探します。

```
Mod_MS_TimeSeriesImport
Mod_KabutanImport_v4
RSS過去データ取り込み
Module7
```

1つずつ **右クリック → 「〇〇 の解放」**
→ 「解放する前に〇〇をエクスポートしますか?」と聞かれたら **「いいえ」**

> **先に消さないと重複します。** 同じ名前のまま取り込むと
> `Module71` のような別モジュールができて、古いほうが動き続けます。
> 前に `modFetch250` と `modFetch2501` が並んだのと同じ現象です。

### 2-2. 新しい4本を入れる

メニュー **ファイル → ファイルのインポート**（`Ctrl + M`）

```
Mod_MS_TimeSeriesImport.bas
Mod_KabutanImport_v4.bas
RSS過去データ取り込み.bas
Module7.bas
```

4本ぶん繰り返します。**1本につき1回だけ**です。

### 2-3. Sheet10 だけは手で直す

シートモジュールはインポートできません。**1文字だけ**なので手で直します。

1. 左のツリーの **Microsoft Excel Objects** の中の `Sheet10` をダブルクリック
2. `Ctrl + F` で **`For c = 5 To 104`** を検索
3. **`104` を `254`** に書き換える

これで終わりです。（`Sheet10_本文.txt` を全部貼り付けても同じですが、
1文字直すほうが早くて安全です）

### 2-4. コンパイルして確認

メニュー **デバッグ → VBAProject のコンパイル**

**何も出なければ成功**です。エラーが出たら、その文言を教えてください。

`Alt + F11` でExcelに戻り、`Ctrl + S` で保存します。

---

## 3. データ取込シートの設定

`データ取込` シートを開いて、2つのセルを直します。

| セル | 入れる値 | 意味 |
|---|---|---|
| **K1** | `400` | RSSに要求する本数 |
| **M1** | `20250712` | 取得の**開始日**（yyyymmdd） |

**M1は「開始日」で、そこから未来方向へ本数ぶん返ります。**
今日（2026/08/06）から420日さかのぼると 2025/07/12 です。
420暦日 ≒ 282営業日なので、250営業日を確実に含みます。

> 日付が変わっても、だいたい **1年ちょっと前の日付**を入れておけば大丈夫です。

---

## 4. 取り込む

マーケットスピードを**起動してログイン**しておきます。

`Alt + F8` でマクロ一覧 → **`マーケットスピード過去データ取込`** を実行。

1. `データ取込` シートの **B1 に銘柄コード**を入れる
2. マクロを実行
3. 確認ダイアログに「取込件数: ○○日分」が出る → **250前後なら成功**
4. 「はい」

まず **1銘柄だけ**試してください。250日入るのを確認してから残りに進みます。

---

## うまくいかないとき

| 症状 | 見るところ |
|---|---|
| 取込件数が 60〜100 日で止まる | `M1` の日付が近すぎます。もっと前にしてください |
| 取込件数が 1 日 | `M1` が今日になっています |
| 「ヘッダ行が見つかりません」 | `データ取込` シートに「日付」「始値」「終値」の行が必要です |
| 書き込めない・1004エラー | 価格シートの保護。**校閲 → シート保護の解除**（パスワード `ne19480314`） |
| コンパイルエラー | モジュールが重複しています。2-1をやり直してください |

---

## まだ変えていないもの

`Mod_売抽出_下降VWAP` と `Mod_買抽出_順張りVWAP` の

```vba
Private Const HIST_DAYS     As Long = 60
Private Const HIST_LAST_COL As Long = 64
```

および `Mod_買抽出v13`（6箇所）・`Mod_SellExtrac`（1箇所）の `104` です。

ここは**指標の計算窓が変わる可能性がある**ので、まず取込で250日入るのを
確認してから手を付けます。
