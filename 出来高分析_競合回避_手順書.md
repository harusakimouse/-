# 出来高分析シート ― 他Excelブックとの「時間競合」回避 手順書

## 何が問題だったか

400番ブックの出来高分析マクロは、`Application.OnTime` で1分ごとに
「時刻チェック」を予約し直しながら記録しています。

```vba
' 【従来】ブック名を付けずに予約している
Application.OnTime Now + TimeValue("00:01:00"), "時刻チェック"
```

`Application.OnTime` は **Excel全体（アプリケーション単位）** で管理され、
発火時刻になると **その時アクティブなブック** からマクロ名を探して実行します。

親ブック（`打ち出のこづち`）も `日次更新_自動`・`売抽出タイマー実行`・
`RSS監視タイマー` など多数の `OnTime` を未修飾で使っているため、
両方を同時に開いていると次のような **時間競合** が起こります。

- 400番が予約した「時刻チェック」の発火時に別ブックがアクティブだと
  **「マクロ '時刻チェック' が実行できません」** エラーになる
- 別ブックにも同名マクロがあると **意図しないブックのマクロが動く**
- 停止処理が正確な発火時刻を指定できず **タイマーを取り消せず残る**

## どう直したか（ブック名で修飾）

OnTime に渡すマクロ名を **「'このブック名'!マクロ名」** の形に修飾しました。
これで予約したタイマーは必ず **そのブック自身** の「時刻チェック」を呼び、
他ブックがアクティブでも競合・誤爆しません。

```vba
' 【修正後】必ず自分自身のブックのマクロを呼ぶ
Private Function MyProc(ByVal procName As String) As String
    MyProc = "'" & ThisWorkbook.Name & "'!" & procName
End Function

Private Sub スケジュール設定()
    NextRunTime = Now + TimeValue("00:01:00")     ' 次回発火時刻を記憶
    Application.OnTime NextRunTime, MyProc("時刻チェック")
    TimerActive = True
End Sub
```

あわせて、**予約した正確な発火時刻を覚えておき**、記録停止のときは
その時刻とブック名付きマクロ名で取り消すようにしました
（従来は時刻がズレて取り消しに失敗することがありました）。

> 記録ロジック・シート構成・記録時刻（09:00〜15:30）は一切変えていません。
> 変更したのはタイマーの「予約／取り消しの仕方」だけです。

## 差し替え手順（1分で完了）

1. 400番ブックを開き、`Alt` + `F11` で **VBAエディタ** を開く
2. 左の「プロジェクト」から **標準モジュール → Module1** をダブルクリック
3. コードウィンドウ内で `Ctrl` + `A`（全選択）→ `Delete`（全削除）
4. `出来高分析_Module1_競合回避版.bas` の中身を丸ごと貼り付け
   - 先頭の `Attribute VB_Name = "Module1"` の行は貼り付け時に自動処理されます。
     もし貼り付け後に赤字で残る場合はその1行だけ削除してください。
5. `Ctrl` + `S` で保存（xlsm 形式のまま上書き）

以上で完了です。翌営業日から「記録開始」を実行すれば、
親ブックを同時に開いていても競合しません。

## （任意）親ブック側もより安全にしたい場合

今回の依頼範囲は400番（出来高分析）ですが、根本的には
親ブック側の `OnTime` も同じ「ブック名で修飾」に直すと、
どちらのブックがアクティブでも完全に安全になります。
必要であれば親ブックの `Module1` / `ThisWorkbook` / `Mod_SellExtrac` /
`Mod_RSS接続状態` も同じ方針で修正できます。
