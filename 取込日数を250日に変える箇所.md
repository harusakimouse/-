# 取込日数を 60/100日 → 250日 に変える箇所（V805のVBA）

E列が最新日なので、**250日 → 最終列 = 5 + 250 - 1 = 254 = IT列**。

V805のVBAには `104`（CZ列＝100日）が直書きされています。
`Mod_売抽出_下降VWAP` と `Mod_買抽出_順張りVWAP` には

```vba
' ★60日→100日に延長する場合はこの2つを 100 / 104 に変更（データ側の延長も必要）
Private Const HIST_DAYS     As Long = 60
Private Const HIST_LAST_COL As Long = 64
```

と、変更箇所そのものがコメントで書いてあります。

---

## A. 取込・移植系（まずここだけ変えれば250日入る）

読み書きする範囲を広げるだけなので、**指標の中身は変わりません**。

| モジュール | 現在のコード | 変更後 |
|---|---|---|
| `Mod_MS_TimeSeriesImport` | `ReDim dateRows(1 To 300)` | `1 To 400` |
| 〃 | `ReDim dateVals(1 To 300)` | `1 To 400` |
| 〃 | `ws.Range(ws.Cells(targetRow, 5), ws.Cells(targetRow, 104)).ClearContents` | `104` → `254` |
| 〃 | `If col > 104 Then Exit For` | `104` → `254` |
| `Mod_KabutanImport_v4` | `ws.Range(ws.Cells(targetRow, 5), ws.Cells(targetRow, 104)).ClearContents` | `104` → `254` |
| 〃 | `If col > 104 Then Exit For` | `104` → `254` |
| `RSS過去データ取り込み` | `For c = 5 To 104` | `To 254` |
| `Sheet10`（シートモジュール） | `For c = 5 To 104` | `To 254` |
| `Module7`（OHLCV移植） | `Private Const C_HISTEND As Long = 104` | `254` |

`Module7` は定数1つ変えるだけで、その下の9箇所すべてに効きます。

### データ取込シート側

```
K1 = 取得本数    M1 = 開始日
```

`M1` は**開始日で、そこから未来方向へ本数ぶん**返ります。250営業日ほしいなら

- `M1` = 今日から **420日ほど前**の日付（`yyyymmdd`）
- `K1` = **400**

を入れてください。返ってきたものを日付の新しい順に並べて、先頭250本を使います。

---

## B. 抽出系（Aのあとで、指標を250日に対応させたいとき）

| モジュール | 現在のコード | 変更後 |
|---|---|---|
| `Mod_売抽出_下降VWAP` | `Private Const HIST_DAYS As Long = 60` | `250` |
| 〃 | `Private Const HIST_LAST_COL As Long = 64` | `254` |
| `Mod_買抽出_順張りVWAP` | `Private Const HIST_DAYS As Long = 60` | `250` |
| 〃 | `Private Const HIST_LAST_COL As Long = 64` | `254` |
| `Mod_買抽出v13` | `Cells(i, 104)` が5箇所＋`Cells(r, 104)` が1箇所 | すべて `254` |
| `Mod_SellExtrac` | `hist = ws.Range(ws.Cells(r, 5), ws.Cells(r, 104)).Value` | `104` → `254` |

> **Bは中身を確認してから変えてください。**
> `HIST_DAYS` は配列の長さです。RSI14・MACDなどの固定窓の指標は影響を受けませんが、
> 「履歴全体の高値／安値」のような**期間を区切らない使い方があると意味が変わります**
> （60日高値 → 250日高値になる）。まずAだけ変えて250日を入れ、
> 動くのを確認してからBに進むのが安全です。

---

## C. 別件：V811の数式参照が中途半端になっています（私のミス）

V811.xlsm の数式を数えたところ、こうなっていました。

```
終値!$E$..:$IT$..    818件   ← 250日に広げ済み
始値!$E$..:$BT$..   1833件   ← 68日のまま
高値!$E$..:$BT$..   3664件   ← 68日のまま
安値!$E$..:$BT$..   3664件   ← 68日のまま
始値!$C$..:$BT$..    458件   ← 68日のまま
```

**終値だけしか広げられていません。** 私が入れた拡張処理が分析／売分析シートにしか
当たっておらず、管理シートの1833件などが漏れています。

このままだと250日ぶん取り込んでも、**高値・安値・始値を使う計算は68日しか見ません**。
`$BT$` → `$IT$` の置換を全シートに当て直す必要があります。
