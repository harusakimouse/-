# 「自動発火してない」調査結果と対応

対象ブック: **打ち出のこづち V0728**（楽天RSS 株価データ 自動取得ツール）

---

## 1. 結論

「自動発火してない」ように見える症状の正体は、**別々の2つの不具合**でした。

| # | 不具合 | 状態 |
|---|--------|------|
| A | 完了判定 `HasToday` が常に False → 30秒ごとに無限に再取得し、**大量の重複行**を生成。ログも毎回「書込0件」と誤表示 | **確定**（データで実証） |
| B | 自動発火チェーン（`Application.OnTime`）が途中で切れると開き直すまで復活しない設計の脆さ → 7/30 は一度も自動取得されず | **設計上の脆弱性** |

---

## 2. 証拠（ログ回数 = 実データの重複行数）

7/29 の「実行ログ」の回数と、各シートの実データ行数が完全に一致していました。

| シート | 7/29 ログ「書込0件」回数 | 7/29 データ行数 | 銘柄93で割ると |
|--------|--------------------------|-----------------|----------------|
| 1500 | 73 | 6,789 | **73回分** |
| 1520 | 47 | 4,371 | **47回分** |
| 1530 | 22 | 2,046 | **22回分** |

→ 「書込0件」とログに出ていたが、**実際は毎回93行フルに書き込み、それを延々繰り返していた**。

---

## 3. 根本原因A（確定）

`Module1` の完了判定：

```vba
If IsNumeric(ws.Cells(lr, 1).Value) Then          ' ← ここが原因
    HasToday = (CLng(ws.Cells(lr, 1).Value) = CLng(Date))
End If
```

A列（日付）は**日付書式**のセル。VBA で `.Value` を読むと **Date 型**で返り、
`IsNumeric(Date型)` は **必ず False**。よって `HasToday` は「本日分あり」を
一度も True にできず、以下が同時発生：

1. `RunOnce` の重複ガード `If HasToday Then Exit Sub` を毎回すり抜け → 30秒ごとに再取得（重複）
2. `成功` が一度も記録されず、毎回「書込0件 → 次回リトライ」と誤表示
3. 手動ボタン側の重複確認ダイアログも出ず、手動でも重複（7/22 が2〜4回分）

---

## 4. 根本原因B（7/30 に一度も自動発火しなかった件）

スケジューラは「30秒後の自分を予約 → 発火 → また30秒後を予約」という**単線チェーン**。
起動〜15:00 の約6時間で約730回連続で回り続ける必要があり、途中で一度でも
（セル編集中／重い再計算中／ダイアログ表示中など）取り逃すと**そのまま停止**し、
ブックを開き直すまで自動復帰しません。監視（ウォッチドッグ）が無いため気付けません。
原因Aの重複でブックが肥大化・再計算が重くなり、途切れやすさを悪化させていました。

---

## 5. 実施した対応

### ✅ 対応1: 既存の重複行を削除（このリポジトリのファイルに適用済み）

`打ち出のこづち_V0728_重複削除済.xlsm`

- 対象: **1500 / 1520 / 1530** シート
- ルール: **(日付, コード) の組み合わせごとに最初の1件だけ残す**
  （最初の取得＝各目標時刻に最も近いスナップショット）
- 結果: 各シート **93銘柄 × 7営業日 = 651行** に正規化（重複ゼロを検証済み）

| シート | 削除前 | 削除後 | 削除行数 |
|--------|-------:|-------:|---------:|
| 1500 | 7,440 | 651 | 6,789 |
| 1520 | 5,301 | 651 | 4,650 |
| 1530 | 2,697 | 651 | 2,046 |

- VBA本体・ボタン・数式・書式・他シート（実行ログ等）は**バイト単位で無変更**。
  対象3シートのデータ行のみを外科的に編集しています。

### ✅ 対応2: VBA修正コードを提供 → `VBA修正_HasToday.bas`

- **修正A（必須）**: `HasToday` を `IsDate` ベースの判定に修正。これで
  重複の無限発生と「書込0件」の誤表示が止まり、`成功` が正しく記録されます。
- **修正B（任意・推奨）**: シート切替時にチェーンの生存を確認して自己復旧。

> VBA はファイルへ自動適用していません。マクロ本体（`vbaProject.bin`）を
> Excel 以外で書き換えると、開いた際に破損＝**全マクロ消失**の恐れがあり、
> Excel 無しでは検証できないためです。`.bas` の手順（Alt+F11 で貼り付け）が
> 安全・確実です（30秒程度）。

---

## 6. あなたに実施していただくこと

1. `打ち出のこづち_V0728_重複削除済.xlsm` を今後の運用ファイルにする
2. そのファイルを開き、`VBA修正_HasToday.bas` の手順で **修正A** を適用（必須）
3. 可能なら **修正B** も追記（自動発火の取りこぼし対策）
