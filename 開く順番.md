# 申し訳ありません。2回とも同じエラーを出しました

この環境には Excel も、代わりに使える LibreOffice もありません
（LibreOffice はこのコンテナでは壊れていて、何も開けません）。
そのため **私は自分で「Excelで開いた結果」を確認できません。**
静的な検査だけを頼りに直しており、それが2回とも足りませんでした。

そこで今回は **原因を1回で切り分けられるように、2つのファイル** を用意しました。
お手数ですが、この順番で開いてみてください。

---

## ① まず `V808_検証A_展開のみ.xlsm` を開いてください

**V805 に対して、共有数式の展開と行の spans / dimension の修正しかしていません。**
数式の書き換えも、列の追加も、250日拡張も、T/U の手入力化も一切していません。
（機能は V805 のままです。開けるかどうかだけを見ます）

| 結果 | 分かること | 次の手 |
|---|---|---|
| **正常に開く** | 展開処理は正しい。原因は私の数式編集側 | ②へ進む |
| **同じエラーが出る** | 展開処理そのものが原因 | 別の方式に切り替えます |

## ② ①が正常なら `V808b.xlsm` を開いてください

こちらが本命です。前回の V808 から次を直しました。

- `<c>` セルの属性（書式 s= / 型 t= / メタデータ cm=）を**一切触らない**ようにした
  （前回は型属性を7,274個ぶん落としていました）
- キャッシュ値も残し、**差し替えるのは `<f>` の中身だけ**にした
- 行の `spans` と `dimension` を実態に合わせて再計算
  （分析・売分析は 206行すべてで宣言と実態がずれていました）
- 管理シートの配列数式3,664個には**触らないことにした**
  （T/U を手入力にしたので、到達日シミュレーション列は使われないため）

| 結果 | 次の手 |
|---|---|
| **正常に開く** | 完成です |
| **エラーが出る** | ①が正常なら原因は数式編集側に絞れているので、そこを潰します |

---

## エラーが出た場合にお願いしたいこと

修復ダイアログの「削除されたレコード」に出た **sheet番号** を教えてください。

| sheet番号 | シート名 |
|---|---|
| sheet1 | 管理 |
| sheet3 | 厳選TOP2 |
| sheet7 | 分析 |
| sheet8 | 売分析 |

どのシートが残り、どれが落ちたかで原因をさらに絞り込めます。

---

## ここまでに確認できていること

前回 V808 で「共有数式の孤児」は確かに直っていました。それでも同じ3シートが
落ちたため、原因は別にあります。今回の切り分けはそれを特定するためのものです。

なお、次の点は静的検査で確認済みです（それでも Excel が弾いた、という状態です）。

```
ZIP整合性 / 全107パーツのXML整形式 / VBAバイナリ完全一致
共有数式の残存ゼロ / 配列数式の完全保持 / 行数・セル数の完全一致
セルの列順序 / 数式長(最長651文字・上限8192) / 括弧ネスト(最深10・上限64)
#REF! ゼロ / 列参照XFD超過ゼロ / 展開結果と期待値の一致 5,355/5,356
```
